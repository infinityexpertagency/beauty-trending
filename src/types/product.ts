export interface Product {
  id: number;
  handle: string;
  image: string;
  category: string;
  title: string;
  price: string;
  compareAtPrice: string;
  badge: string;
  badgeColor: string;
  description: string;
  shopifyUrl: string;
}